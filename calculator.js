// Create a module for adding and subtracting
const Calculator = {
  add: (a, b) => {
    return a + b;
  },

  subtract: (a, b) => {
    return a - b;
  }
};

// Export the module
module.exports = Calculator; 

